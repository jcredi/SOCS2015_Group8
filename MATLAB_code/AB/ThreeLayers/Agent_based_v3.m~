!synclient HorizTwoFingerScroll=0

kappa = 0.01;
width = 0.2;
runs = 1e0;
probabilitiesCW = exp(-distances{1}.^2 / width^2);
probabilitiesWS = exp(-distances{2}.^2 / width^2);
requestedGoodsCW = zeros(size(probabilitiesCW));
for k = 0:runs
    requestedGoodsCW = RequestGood(retailersDemands,probabilitiesCW);
    
end

disp(requestedGoodsCW);