function outputProbabilities = InitialiseProbabilities(distanceArray)
% InitialiseProbabilities 
dimensions = size(distanceArray);
outputProbabilities = zeros(dimensions);
for i = 1:length(dimensions)
    for j = 1:dimensions(i)
        outputProbabilites()
    end
end

end